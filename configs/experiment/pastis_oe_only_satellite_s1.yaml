# @package _global_

# to execute this experiment run:
# python train.py experiment=mmdc_full

defaults:
  - override /datamodule: pastis_datamodule_oe_only_satellite_S1.yaml
  - override /model: pastis_oe_utae_s1.yaml
  - override /callbacks: default.yaml
  - override /trainer: default.yaml

output_dir: /work/scratch/data/${oc.env:USER}/MMDC/results/pastis


# all parameters bellow will be merged with parameters from default configurations set above
# this allows you overwrite only specified parameters

tags: ["pastis_utae"]

name: "pastis_utae"
